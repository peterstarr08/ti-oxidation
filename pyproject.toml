[build-system]
requires = ["setuptools>=77.0.3"]
build-backend = "setuptools.build_meta"

[project]
name = "Ti_Oxidation"
version = "0.1.0"

[project.scripts]
rdf_layer = "ti_oxidation.rdf_layer_corrected:main"

[tool.pytest.ini_options]
pythonpath = ["src"]

